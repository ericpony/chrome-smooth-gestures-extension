A clean, ad-free Smooth Gesture Chrome Extension 

Linux/Mac users have to install the plugin (as a standalone Chrome extension) to support the following functions:

1. Switch to Fullscreen mode
2. Open the developer tools for the current page
3. Save the current page
